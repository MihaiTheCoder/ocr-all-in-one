apiVersion: v1
kind: Service
metadata:
  name: {{ template "ocrwrapperwebapi.fullname" . }}
  labels:
    app: {{ template "ocrwrapperwebapi.name" . }}
    chart: {{ template "ocrwrapperwebapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "ocrwrapperwebapi.name" . }}
    release: {{ .Release.Name }}
